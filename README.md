# Pyxcel
Excel, but in Python.

## Prerequisites

Install the required package using the following command:

- Using pip:

```bash
pip install pyyaml
```

- Using conda:

```bash
conda install pyyaml
```
